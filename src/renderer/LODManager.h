#pragma once
// LODManager has moved to src/world/LODManager.h (M10: neutral streaming tier,
// docs/ARCHITECTURE.md §13). This header is kept for backward compatibility with
// demos and tests that include "renderer/LODManager.h".
#include "world/LODManager.h"
